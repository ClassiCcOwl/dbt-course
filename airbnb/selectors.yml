selectors:
  - name: dim_expect_listings_w_hosts
    description: "Select the dim_expect_listings_w_hosts model"
    definition:
      union:
        - method: path
          value: models/dim
        - exclude:
            - method: fqn
              value: dim_expect_listings_w_hosts
